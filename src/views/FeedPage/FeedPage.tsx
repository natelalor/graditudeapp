import { Typography } from '@material-ui/core';

import styles from './FeedPage.module.scss';


export function FeedPage() {
    return (
        <div className={styles.root}>

            <Typography
                className={styles.title}
                variant="h1"
            >
                Be Grateful.
            </Typography>

            <Typography>
                apriala / apriell / aphriel / gracael / thancia / appreciosa / apreja / apseja / ankja / thankja / gratija / aprieva {/* eslint-disable-line */}
            </Typography>
        </div>
    );
}
